﻿﻿# SnipPlus v1 Requirements-to-Code Conformance Matrix

狀態：`Stage 5 Repository owner manual acceptance passed for the Owner Reference environment with the documented Windows PrintScreen compatibility prerequisite; Stage 6A／6B／6C implementation and deterministic evidence include Selection adjustment、Function Bar、Complete gate、same-frame final render、Clipboard delivery and typed Complete failure tracing／recovery; the packaged Complete failure was traced to CO_E_NOTINITIALIZED at the exact WinRT Clipboard call, the first initialization correction exposed RPC_E_CHANGED_MODE for the selected apartment mode, and the source now uses a synchronous WinUI STA entry point with the required Windows Runtime boundary and deterministic build／test evidence; Repository owner has confirmed one current Owner Reference packaged Complete／paste scenario with matching selected content／position; Stage 7A–7I provide the accepted Annotation／pointer-editing／history foundations, Stage 7J／7K provide annotation-aware final rendering／Complete integration, Stage 7L provides the explicit output commitment gate, Stage 7M provides the reusable Core／Windows PNG Save As foundation, and Stage 7N integrates production Save through one canonical result／ResultId into PNG and Clipboard with deterministic evidence; packaged Save／native picker runtime、broader four-4K／performance and retained-file-reference evidence remain pending; Stage 7 is not complete`

本次 Stage 7N 已將 Stage 7M 的 PNG Save As foundation 接入 production Function Bar Save：Save 只在有效 `Editing` 狀態可用，與 Complete 共用單一 output gate，重新驗證 Session／coordinate／Selection／Annotation／FrozenDisplayFrameSet／capacity，精確 render 一次 canonical `IImageResult`，並以同一個 image／`ResultId` 完成 PNG write evidence 與 `SuccessfulSave` Clipboard commitment。Picker cancel、encode／write failure、PNG 成功後 Clipboard failure 的 Editing recovery 與狀態保留均有 deterministic Core evidence；未加入 dedicated retained-file reference，因此 `FR-050` 仍為 `Partial`。Locked restore、Release x64 build、filtered non-interactive tests `260/260` 與 `git diff --check` 有證據；modified C# files 的限定 format verification 只剩既有 LF→CRLF `ENDOFLINE` baseline，未執行 packaged／native Save runtime，Stage 7N 為 `Partial／runtime Pending`，Stage 7 仍未完成。

## 1. Document Control

| Field | Value |
| --- | --- |
| Document ID | `PRD-TRACEABILITY-MATRIX-001` |
| Version | `3.13` |
| Review date | `2026-08-19` |
| Product baseline | Accepted current `PRD-0004`–`PRD-0006` and `SPEC-0003`–`SPEC-0010` |
| Reviewed implementation | Branch
emove-snipplus-md` technical prototype |
| Review type | Static requirements-to-code／test／runtime conformance review |
| Code changes authorized by this document | No |

This matrix compares accepted v1 behavior with current code、tests and historical runtime evidence. Passing builds or test counts do not prove user-visible conformance.

## 2. Status Definitions

| Status | Meaning |
| --- | --- |
| `Conforms` | Current implementation directly satisfies the requirement with relevant evidence. |
| `Partial` | A reusable portion exists, but accepted behavior or coverage is incomplete. |
| `Missing` | No implementation of the accepted behavior was found. |
| `Incorrect` | Current behavior conflicts with the accepted baseline. |
| `Obsolete` | Behavior belongs only to the superseded workflow. |
| `Blocked by product decision` | An unresolved visible product decision prevents implementation. No current v1 row has this status. |

## 3. Executive Result

Current source is a reusable **single-display capture／crop／PNG／Clipboard technical prototype with statically verified four-display topology／freezing integration、fifth-slice all-display presentation／initial Selection foundations、Stage 6A SelectionLocked adjustment and Stage 7A–7N Annotation／pointer-creation／object-editing／history／Save foundations**, not yet a conforming SnipPlus v1 implementation.

Reusable foundations:

- one-display WGC acquisition;
- same-frame crop;
- clear-inside／dim-outside mask on one surface;
- single-display coordinate conversion;
- BGRA8 premultiplied SoftwareBitmap;
- PNG encoder;
- Clipboard delivery with bounded cancellable retry;
- shared state authority;
- platform-neutral four-4K capacity policy、typed capacity outcomes、Frozen Virtual Desktop／Display snapshots and transparent gap policy;
- platform-neutral Capture Session context、per-display frozen frame set ownership、single `CaptureRequested → Freezing` transition and deterministic cleanup boundary;
- resident lifecycle coordinator、persisted takeover setting and platform-neutral PrintScreen boundary;
- Windows PrintScreen registration／release adapter and direct application-exit cleanup path;
- platform-neutral PrintScreen／secondary capture-request boundary、formal `ResidentReady` initial state and `ResidentReady → CaptureRequested` transition;
- Windows display topology mapping、physical Virtual Desktop bounds、DPI／orientation metadata、mirror dedupe and deterministic coordinate version;
- per-display Windows.Graphics.Capture adapter lifecycle、all-display prepare／start／parallel first-frame coordination、typed integration outcomes and complete frame-set cleanup;
- deterministic low-level tests.
- SelectionLocked move、eight physical hit-test handles、edge／corner resize、outside-drag reselection、revision／rollback semantics and Windows handle／cursor mapping with deterministic evidence.
- platform-neutral `Editing` transition、Function Bar placement／presentation／command contracts、physical Work Area／DPI placement and deterministic Stage 6B workflow evidence.
- platform-neutral Complete gate、`Editing → ResultReady → Delivering → Completed → ResidentReady` transitions、same frozen frame-set final-render contract and retained-Editing failure boundary.
- Windows frozen-frame-set compositor that creates one canonical BGRA8／premultiplied／sRGB SDR result, copies only display intersections and leaves non-display gaps transparent; existing bounded WinRT Clipboard delivery is now behind Complete.
- platform-neutral Annotation Document、Object identity、physical-pixel geometry、independent Annotation Revision and typed mutation boundary with deterministic ownership／cleanup evidence.

Major implementation gaps:

- topology-change runtime verification and a stable runtime cancellation interruption;
- packaged Windows runtime verification of the Stage 6A SelectionLocked adjustment、Stage 6B Editing／Function Bar and Stage 6C Complete／Clipboard;
- broader four-4K capacity、transparent-gap runtime and performance evidence;
- packaged annotation-aware final render／Clipboard runtime evidence; Stage 7H object editing、Stage 7I Annotation-only history、Stage 7J annotation-aware compositor and Stage 7K explicit Complete／capacity／canonical-result integration source／deterministic evidence now exist;
- explicit Complete／Successful Save commitment boundaries and progress state;
- packaged/native Save As／PNG／Clipboard runtime behavior and the dedicated retained-file reference outcome;
- quantitative timing、memory and repeated-session evidence;
- the later Capture workflow runtime evidence;
- focus restoration and retained Editing after recoverable failure.

Keyboard-only Annotation and non-PrintScreen shortcuts are deferred and must not be classified as missing v1 work.

## 4. Functional Requirements Conformance

| Requirement | Accepted behavior | Current result | Status | Required action |
| --- | --- | --- | --- | --- |
| `FR-001` | Manual startup keeps SnipPlus resident while running. | `ResidentLifecycleCoordinator`、single-instance activation and direct MainWindow exit are implemented; Repository owner manual acceptance confirmed Esc keeps the process resident, repeated sessions work, resident activation reuses the existing MainWindow and MainWindow `X` fully exits. | `Conforms` | Preserve the resident entry and exit boundary. |
| `FR-002` | User controls PrintScreen takeover. | `IPrintScreenTakeover`、persisted settings store、Windows `RegisterHotKey`／`UnregisterHotKey` and the application-owned message-only HWND are implemented. Repository owner confirmed background registration and repeated enable／disable behavior when the documented Windows native PrintScreen setting is disabled. Deterministic tests cover settings、idempotent registration／release and failure state. | `Conforms` | Preserve the registration boundary and document the Windows compatibility prerequisite. |
| `FR-003` | When takeover is enabled and SnipPlus is resident, PrintScreen starts one capture session. | PrintScreen maps to a platform-neutral `CaptureRequest` and enters `COMP-001` exactly once through `ResidentReady → CaptureRequested`; the accepted request has a static formal path through `CaptureRequested → Freezing` to all-display presentation、`Selecting`、`SelectionLocked` and Stage 6B `Editing`, with deterministic tests. Owner Reference packaged runtime accepted the fifth-slice Overlay／initial Selection; Stage 6A／6B runtime remains pending. | `Partial` | Execute the separately authorized packaged adjustment／Editing runtime verification. |
| `FR-004` | Disabled takeover or application exit does not intercept PrintScreen. | Resident lifecycle release and late-event boundaries are deterministic-tested. Repository owner confirmed disabled takeover stops SnipPlus receipt, post-Esc background PrintScreen can start a new session, and MainWindow `X` leaves no SnipPlus process. The documented Windows native PrintScreen setting controls whether Windows itself shows its capture surface. | `Conforms` | Preserve the release and exit boundary. |
| `FR-005` | In-app capture is secondary. | Start Capture creates `SecondaryInAppCommand` and uses the same Core request boundary as PrintScreen; Repository owner repeated the secondary entry through the accepted Stage 5 overlay／initial Selection flow and cancelled without output. | `Conforms` | Preserve the shared request boundary; later output stages remain separate. |
| `FR-046`–`FR-047` | MainWindow `X` and explicit Exit terminate、release takeover and do not hide to tray. | MainWindow `Closed` still calls the application-exit boundary before `Environment.Exit(0)`; no tray surface exists; deterministic exit／Dispose tests pass. The current custom `AppInstance` and message-only owner package has not yet received the owner’s current packaged X／process cleanup re-acceptance. | `Partial` | Re-run current packaged MainWindow X and process cleanup verification. |
| `FR-006`–`FR-010` | Freeze and present all supported displays、crosshair、cross-monitor Selection and mask. | Owner Reference three-display topology、physical bounds／DPI、per-display WGC frame set、Frozen Overlay、mask、single system Crosshair and single／cross-display initial Selection passed Repository owner manual acceptance. Maximum four-4K runtime remains unverified. | `Partial` | Preserve the accepted Owner Reference result and separately verify the broader four-4K envelope. |
| `FR-011` | Mouse release locks Selection and creates no output. | Initial valid release is routed through `COMP-001` from `Selecting → SelectionLocked`; current packaged runtime verified single／cross-monitor mouse release locks the selection without crop、Clipboard、PNG or output. Stage 6B retains the SelectionLocked status while entering `Editing`; deterministic tests confirm the Function Bar boundary still has no output path. | `Conforms` | Preserve the no-output SelectionLocked／Editing boundary. |
| `FR-012` | Locked Selection supports pointer move、edge／corner resize and reselection. | `SelectionInteractionMode`、physical hit-testing for eight handles、clamped move、normalized／flipping resize、outside-drag reselection、invalid-release rollback and `SelectionRevision` are implemented in Core; Windows overlays expose physical handle placement and cursor mapping. The shared session input boundary normalizes PointerId across Overlay windows and deduplicates native/XAML release; deterministic tests cover cross-display release, Virtual Desktop bounds and the adjustment contract. Packaged Windows runtime verification is pending. | `Partial` | Execute Owner Reference packaged Stage 6A move／resize／reselection and cleanup verification. |
| `FR-048` | Non-display gap pixels are transparent. | Windows final compositor allocates only the requested Selection raster, copies each intersecting frozen display without stretch and leaves the synthetic／physical gap bytes at BGRA alpha `0`; deterministic negative-coordinate／gap pixel tests pass. Packaged runtime remains pending. | `Partial` | Verify gap pixels in the packaged final result. |
| `FR-013`–`FR-016` | Mandatory Editing function bar and Complete／Save／Cancel. | Stage 6B adds the platform-neutral `SelectionLocked → Editing → Cancelled` boundary and hosted Function Bar placement／cleanup. Stage 6C enables Complete only for a current valid locked Selection and routes completion through the single workflow authority. Stage 7I enables Annotation-only Undo／Redo only in Editing without an active draft, carries the current `AnnotationRevision` in the command request and preserves the existing Complete／Cancel gate. Stage 7N enables Save only for a valid current Editing context with no draft／output in flight, routes it through `Editing → ResultReady → Saving → Delivering → Completed`, and shares the single output gate with Complete. Deterministic Core／Windows tests cover availability, revision-aware dispatch, one-render Save identity, picker cancellation and recoverable failure; packaged runtime cases remain pending. | `Partial` | Preserve the Complete／paste path and verify packaged Editing／Save／Cancel behavior. |
| `FR-017`–`FR-023` | Required pointer-driven Annotation tools and styling. | Stage 7B adds Rectangle creation／preview. Stage 7C adds Arrow／Line creation／preview. Stage 7D adds semi-transparent Highlighter creation／preview. Stage 7E adds Text draft／Unicode content／commit／native editor. Stage 7F adds Mosaic／Blur Privacy Region creation／frozen-frame preview. Stage 7G adds immutable Numbered Marker number／style contracts, Core-owned sequential／configurable next-number state, preserved gaps, pointer draft／commit／cancel, Function Bar `NumberBox` and per-display solid circle／centered-number preview. Stage 7H adds deterministic topmost object selection, pointer move, applicable resize, restyle, Text draft editing and delete with one `AnnotationDocumentCoordinator` revision boundary. Stage 7J adds committed-object final-render support for all six types and Stage 7K integrates those six committed kinds into explicit Complete; packaged tool／IME／final-output runtime acceptance remains unverified. | `Partial` | Preserve creation／preview／Complete boundaries and verify packaged tool、IME and final-output behavior. |
| `FR-024`–`FR-025` | Function-bar Annotation-only Undo／Redo. | Stage 7I adds immutable capture-session history entries for Add／Remove／Replace／atomic Numbered Marker creation with NextNumber transition and NextNumber-only changes; Core validates Session／CoordinateVersion／SelectionRevision／AnnotationRevision, replays exact ObjectId／Z-order／content／style／geometry through `AnnotationDocumentCoordinator`, invalidates Redo on forward mutation, preserves history on recoverable Complete failure and clears it on terminal session cleanup. Function Bar Undo／Redo uses the existing single dispatch boundary and is disabled during active drafts. Contracts／Core／Workflow／Windows deterministic tests pass; packaged runtime is not verified. | `Partial` | Verify packaged Function Bar Undo／Redo and retain the history boundary before later final rendering. |
| `FR-026`–`FR-029` | Frozen Virtual Desktop Annotation coordinates and clipping. | Stage 7B keeps Rectangle geometry、Stage 7C keeps Arrow／Line endpoints、Stage 7D keeps Highlighter polyline points、Stage 7E keeps Text anchors／bounds、Stage 7F keeps Privacy Region geometry、Stage 7G keeps Numbered Marker bounds／center placement and Stage 7H keeps selected／preview／resized objects in physical Frozen Virtual Desktop coordinates independent of Selection revision. Stage 7J adds a Windows compositor that reuses the frozen frame set, projects exact physical coordinates into the selection-sized result, clips committed objects without mutating the document and leaves non-display gaps transparent; Stage 7K consumes the same immutable document／frame context from explicit Complete. Deterministic synthetic evidence passes, while packaged runtime remains pending. | `Partial` | Preserve the immutable document／frame boundary and verify packaged annotation-aware Complete. |
| `FR-030` | Complete renders current revision and writes Clipboard only. | Empty Annotation Documents use the existing base renderer; non-empty documents use the Stage 7J annotation-aware compositor through the Stage 7K explicit Complete path. Stage 7L routes publication through the Core-owned `IOutputCommitmentCoordinator`; only `OutputCommitmentAuthorization.Complete` can authorize Complete publication, and the gate validates `Delivering` state、Session／Result identity、Selection／Annotation revisions、canonical result lifetime and output dimensions before forwarding the existing bounded `IClipboardDeliveryService`. Stale／capacity／render／result／gate failures retain Editing without Clipboard or file output. Deterministic Contracts／Core／Windows evidence passes (`244/244`); packaged annotation-aware Complete／Clipboard behavior remains pending. | `Partial` | Preserve the one-authority Complete／Clipboard commitment gate and verify packaged non-empty output. |
| `FR-031`、`FR-049` | Save As is PNG-only、starts in Downloads、uses timestamp name and permits changes. | Stage 7M adds Core canonical identity／lifetime validation and invariant timestamp filename policy plus a Windows `FileSavePicker` foundation with Downloads start location、PNG-only filter／default extension、selected-target validation and exact stream write／flush through the existing `PngEncoder`. Stage 7N integrates the Function Bar Save command and preserves the picker-selected destination／filename through the production flow; packaged/native picker behavior remains pending. | `Partial` | Verify the packaged/native picker and Save output. |
| `FR-032` | Save sends the same final result to PNG and Clipboard. | Stage 7N renders one canonical `IImageResult` and passes the same object／`ResultId` to `PngSaveCoordinator`; after matching `PngWriteSucceededEvidence`, the same result／identity is sent through `OutputCommitmentAuthorization.SuccessfulSave` to Clipboard. Deterministic tests assert one renderer call, matching PNG／evidence／Clipboard identity and one Clipboard publication; packaged output remains pending. | `Partial` | Verify the packaged Save／PNG／Clipboard path. |
| `FR-033` | Save As cancel returns to Editing. | Stage 7N maps typed `PngSaveResult.SaveDialogCancelled` to `Editing`, keeps the active Session／Selection／Annotation state, produces no PNG／Clipboard output and suppresses error feedback. Deterministic Core evidence passes; packaged picker runtime remains pending. | `Partial` | Verify packaged picker cancellation and Editing recovery. |
| `FR-034`、`FR-045` | Recoverable output failure preserves Editing and actionable feedback. | Final render／Clipboard failures transition `ResultReady`／`Delivering` back to `Editing`, retain the Session／Selection／FrozenDisplayFrameSet, dispose only the temporary final result and now publish mapped feedback inside the visible Function Bar after immediate `Reposition → Show`. Deterministic tests cover both failure classes, one-appearance recovery and trace-sink failure isolation. The packaged failure path now includes the synchronous WinUI entrypoint correction in addition to the COM dispatcher／payload boundary corrections; post-fix packaged failure／success and visible-feedback runtime evidence remain pending. | `Partial` | Verify the post-fix local JSONL stage and visible packaged feedback; preserve later Annotation retention. |
| `FR-050` | Clipboard failure after PNG success retains PNG and returns to Editing. | Stage 7N leaves the user-selected PNG destination untouched after Clipboard failure, returns to `Editing` and preserves the active Session／Selection／Annotation／history／frame state; deterministic Core evidence passes. A dedicated `RetainedFileReference`／retained-output feedback surface is not implemented. | `Partial` | Complete the retained-output reference／feedback contract only if explicitly brought into scope; never roll back PNG. |
| `FR-035` | Success is silent. | Complete success does not publish a success status; the command acknowledgement is internal to the Function Bar boundary and cleanup returns to resident state. Packaged runtime and later focus restoration remain pending. | `Partial` | Verify packaged silent success and later focus restoration. |
| `FR-036`–`FR-038` | Esc cancels at accepted capture stages. | Platform-neutral Esc routing is statically implemented for `Freezing`、`Selecting`、`SelectionLocked` and Stage 6B `Editing`; Function Bar／overlay cleanup is idempotent and deterministic tests pass. Repository owner confirmed pre-drag、drag and SelectionLocked Esc close all overlays, keep the resident process alive, do not reopen MainWindow and permit a new background PrintScreen session. Stage 6B packaged runtime remains pending. | `Partial` | Preserve the accepted Stage 5 behavior and verify Editing cleanup separately. |
| `FR-039` | Cancel produces no output. | Fifth-slice cancellation disposes the session／frame set without crop、Clipboard、PNG or output in deterministic tests; current packaged runtime kept Clipboard sequence unchanged and produced no output during cancellation. | `Partial` | Preserve no-output cancellation through later stages. |
| `FR-040`–`FR-043` | Cleanup、focus restoration、no MainWindow reopening、window exclusion. | Source exclusion／hide is statically performed before frame acquisition; deterministic overlay/session cleanup and no-output tests pass. Repository owner confirmed active capture is not disturbed by reactivation, Esc does not reopen MainWindow, and MainWindow `X` fully exits. Full focus restoration after later output stages remains unimplemented. | `Partial` | Preserve the accepted Stage 5 lifecycle and add later focus restoration separately. |
| `FR-044` | Failures are never reported as success. | Typed Complete stages retain `FailureCode`／category／HRESULT／component and mapped Render／Clipboard／Selection feedback is shown in Capture UI; no success notification is emitted on failure. Deterministic evidence passes, and the packaged `ClipboardPublicationRejected`／`CO_E_NOTINITIALIZED` failure was recorded without false success. The source now uses a synchronous WinUI STA entrypoint for single-instance routing, initializes the Windows Runtime with the matching STA mode and creates the complete Clipboard payload at the actual boundary; packaged success／failure behavior remains pending. | `Partial` | Verify the post-fix packaged trace and retain the no-false-success boundary. |
| `FR-D01`–`FR-D09` | Deferred tools、formats and keyboard-only shortcuts remain absent. | No prohibited deferred implementation found. | `Conforms` | Preserve exclusion. |

## 5. Non-functional Conformance

| Requirement area | Accepted obligation | Current result | Status | Required action |
| --- | --- | --- | --- | --- |
| `NFR-001`–`NFR-003` | p95 capture、pointer interaction、output、progress and memory targets. | Async foundations exist; no accepted harness or evidence. | `Missing／Partial` | Add timers、30-run reporting and memory scenarios. |
| `NFR-004`–`NFR-008` | Stable Session and output integrity. | Immutable platform-neutral Session context、per-display frozen frame-set ownership、all-display overlay plan、same-frame presentation／Selection／final-render reference、partial-failure cleanup and idempotent disposal have deterministic synthetic evidence. Stage 7N renders one canonical result for Save and carries the same image／`ResultId` through PNG write evidence and Clipboard commitment; picker／PNG／Clipboard failure recovery preserves the active state. Packaged output runtime remains pending. | `Partial` | Preserve ownership through runtime Selection、render and output slices. |
| `NFR-009`–`NFR-013` | Cross-display correctness、four-4K capacity and typed over-limit failure. | Four-4K policy、negative／mixed-DPI／irregular-gap snapshots、Windows topology mapping、current Owner Reference per-display WGC lifecycle、fifth-slice physical overlay／selection geometry and Stage 6A physical adjustment／boundary contracts are implemented with static／deterministic evidence. The shared input boundary covers cross-Overlay PointerId normalization and one native/XAML release commit; Owner Reference deterministic geometry permits legal display-to-gap coverage and clamps the outer Virtual Desktop bounds. Repository owner accepted the three-display Owner Reference overlay／Crosshair／initial Selection runtime; Stage 6A adjustment runtime、broader four-4K envelope、topology-change runtime and stable cancellation interruption remain unverified. | `Partial` | Preserve the accepted Owner Reference result and execute Stage 6A adjustment runtime before broader envelope verification. |
| `NFR-014`–`NFR-019` | Familiar interaction、silent success、progress and retained-error feedback. | All-display dim-outside／clear-inside mask、crosshair and initial drag geometry are statically implemented. Stage 6B adds the hosted Editing Function Bar and typed Cancel boundary; Stage 6C adds Complete gate、same-frame composition、silent success cleanup、typed retained-Editing failure feedback and local stage tracing. Stage 7N adds Save availability／one-output gating、picker cancellation without feedback and typed PNG／Clipboard failure recovery back to Editing. The packaged failure path includes the synchronous WinUI entrypoint and COM dispatcher／payload corrections; these fixes are build／test verified. Progress、post-fix packaged Save output／failure evidence and focus restoration remain incomplete. | `Partial` | Verify packaged Save／failure behavior and add later progress／focus evidence. |
| `NFR-020`–`NFR-023` | Focus and exit behavior. | Direct MainWindow X exit、takeover release、custom AppInstance activation routing and resident activation boundary are statically implemented; deterministic lifecycle／activation tests pass. Repository owner confirmed background PrintScreen、pre-drag Esc、MainWindow reactivation、resident process retention and process cleanup for Stage 5. Full focus restoration after later output stages belongs to a later slice. | `Partial` | Preserve the accepted Stage 5 behavior and add later focus restoration separately. |
| `NFR-024`–`NFR-028` | Privacy and verification boundaries. | Local-only and synthetic-evidence boundaries exist. | `Conforms` | Preserve. |
| `NFR-029`–`NFR-031` | Accessible names、Esc cancellation and non-color-only state. | Function Bar Selection／Rectangle／Text controls now expose accessible names and RadioButton selected state without relying on color; the native TextBox／Commit／Discard controls also expose names; Esc remains a whole-session cancellation boundary. Keyboard-only Annotation workflow remains deferred. | `Partial` | Preserve the pointer-driven boundary and add only later accepted accessibility evidence. |
| `NFR-032`–`NFR-036` | Maintainability and traceability. | Canonical documents、single `WorkflowStateAuthority`、platform-neutral request／freezing／presentation／selection／Function Bar／final-render／PNG-save／output-commitment boundaries、Stage 7A Annotation Document／mutation boundary、Stage 7B–7G creation／draft／commit boundaries、Stage 7H object selection／preview／mutation boundary、Stage 7I history boundary and Stage 7J／7K annotation-aware render／Complete boundary exist with deterministic capacity／session／ownership／geometry／placement／render／delivery／Save tests. Stage 7N keeps the document immutable, consumes one snapshot through COMP-001 and proves one canonical Save result／ResultId through PNG／Clipboard; packaged output and performance evidence remain pending. | `Conforms／Partial` | Trace every corrected slice and preserve COMP-001 ownership. |
| `NFR-037`–`NFR-039` | Windows v1 four-4K envelope and deferred compatibility. | Platform-neutral four-4K policy and Windows topology／multi-display WGC integration are implemented; current Owner Reference three-display topology and real WGC freezing passed, while four-display runtime and topology-change runtime remain unverified. | `Partial` | Preserve the verified Owner Reference result and keep broader envelope verification separate. |

## 6. Code Classification

| Current asset | Classification | Disposition |
| --- | --- | --- |
| One-display `WindowsGraphicsCaptureAdapter` | `Partial` reusable foundation | Retained for legacy `ICaptureService`; now also supports one-display prepare／start／first-frame ownership under the four-4K multi-display orchestrator. |
| `WindowsDisplayTopologyProvider`／`WindowsDisplayTopologyMapper` | `Conforms` for current Owner Reference topology integration | Maps Windows physical display data to platform-neutral snapshots with DPI、orientation、negative coordinates、mirror dedupe and deterministic coordinate versions; current Owner Reference three-display runtime verified. |
| `WindowsFrozenDisplayFrameSetProvider`／`WindowsDisplayCaptureAdapterFactory` | `Conforms` for current Owner Reference freezing integration | Creates one adapter per display after Core capacity validation, prepares all, starts all, collects first frames in parallel and cleans every partial resource; current Owner Reference real WGC frame-set acquisition and three-session re-entry verified. |
| `FrozenCaptureFrame` | `Partial` reusable foundation | Evolve into Session-owned per-display frames. |
| `CoordinateMapper` | `Partial` reusable foundation | Add Virtual Desktop intersections、mixed-DPI mapping and transparent gaps. |
| `AnnotationDocument`／`AnnotationDocumentCoordinator` | `Partial` Stage 7A–7K foundation | Preserve one Session-owned immutable document、typed Rectangle／ArrowLine／Highlighter／Text／PrivacyRegion／NumberedMarker content／style、physical-pixel object geometry、independent Annotation Revision、typed stale／mutation outcomes and cleanup. Stage 7B–7G add Core-owned active-tool／draft／commit coordination and presentation-only clipping／frozen-frame effect／marker preview; Stage 7H adds Core-owned selected-object preview、one Replace／Remove revision boundary、style changes、Text edit draft and delete; Stage 7I adds Core-owned immutable history and replay without a second document authority; Stage 7J adds the separate annotation-aware render request／result boundary and Windows compositor; Stage 7K consumes an immutable document snapshot during Complete without making the document mutable or creating a second authority. Packaged runtime and Save output integration remain later work. |
| One-display mask | `Partial` reusable behavior | Preserve clear-inside／dim-outside semantics. |
| `SelectionCanvas_PointerReleased → CompleteSelectionAsync` | `Obsolete` | Removed from the MainWindow product entry; no fifth-slice release-to-output path remains. |
| Current `WorkflowState` graph | `Partial` | Formal `ResidentReady → CaptureRequested → Freezing → Selecting → SelectionLocked → Editing → ResultReady → Saving → Delivering → Completed → ResidentReady` transitions are now legal through the single authority, with cancellation／failure return to `Editing` covered by deterministic tests. Stage 7N adds the Save state and tests its shared output gate with Complete; packaged runtime remains pending. |
| Frozen frame-set final compositor／Complete orchestration | `Partial` | Stage 6C validates the current locked Selection, composes only intersections from the same Session-owned `FrozenDisplayFrameSet`, preserves transparent gaps and routes the canonical result through the Core-owned `IOutputCommitmentCoordinator`; Stage 7J adds the annotation-aware compositor and Stage 7K wires it into explicit Complete for non-empty immutable Annotation Documents while preserving the empty-document base path. Stage 7L removes direct Clipboard service ownership from Complete, validates exact Session／Result／Selection／Annotation identity and keeps the bounded retry adapter behind the gate. Final topology／Selection capacity and canonical result metadata are revalidated; typed stage／failure tracing and retained Editing recovery are deterministically tested. Packaged annotation-aware Complete／Clipboard behavior remains pending. |
| `OutputCommitmentAuthorization`／`OutputCommitmentCoordinator` | `Partial` | Stage 7L provides the platform-neutral explicit Complete commitment and Stage 7N invokes the typed Successful Save commitment only after matching `PngWriteSucceededEvidence`. The Core gate is the only source owner of `IClipboardDeliveryService`, enforces `WorkflowState.Delivering`, exact canonical identity and matching PNG-success evidence, and preserves History／roaming privacy defaults and bounded retry／failure semantics. Packaged output runtime remains pending. |
| Clipboard retry adapter | `Conforms` technical foundation | Preserve bounded retry and privacy defaults. |
| `PngEncoder` | `Partial` reusable foundation | Existing encoder is reused by Stage 7M Windows Save As persistence; verify packaged alpha／size tiers and later final-result integration. |
| `PngSaveRequest`／`PngSaveCoordinator`／`WindowsPngSaveService` | `Partial` Stage 7M foundation plus Stage 7N production integration | Core owns canonical identity／lifetime／timestamp policy; Windows owns Downloads／PNG picker、stream write／flush and typed cancellation／failure outcomes. Stage 7N integrates Function Bar Save, one canonical render, matching PNG／Clipboard identity and recoverable Editing outcomes; packaged/native runtime and dedicated retained-file reference remain pending. |
| Current workflow tests | `Partial` historical foundation | Supersede release-to-Clipboard assertions and add v1 quality tests. |
| `ResidentLifecycleCoordinator` and PrintScreen contracts | `Conforms` for Stage 5 Owner Reference with the documented Windows prerequisite | Added setting、registration、event-boundary and exit cleanup ownership; deterministic non-interactive evidence and Repository owner background／Esc／exit acceptance pass. |
| `WindowsPrintScreenTakeover` | `Conforms` for Stage 5 Owner Reference with the documented Windows prerequisite | Added Win32 registration／release and an application-owned message-only HWND boundary; deterministic adapter tests and owner background `WM_HOTKEY`／release acceptance pass when the Windows native PrintScreen setting is disabled. |
| `ISettingsLauncher`／`WindowsSettingsLauncher` | `Conforms` for Stage 5 compatibility guidance | Platform-neutral async settings boundary、typed success／failure result、official `ms-settings:easeofaccess-keyboard` adapter and deterministic fake-launch tests exist; no Registry or takeover mutation is performed. |
| `Program`／`AppInstance` and `ResidentActivationBoundary` | `Partial` for Stage 5 single-instance activation | Custom synchronous `[STAThread] Program.Main` registers fixed key `SnipPlus.Main` before XAML initialization; secondary activation redirection runs on a worker thread and completes through `CoWaitForMultipleObjects`, then exits without creating a second MainWindow or resident lifecycle. The Core activation boundary deterministically ignores active capture／application exit and shows only ResidentReady. Current packaged Start-menu reactivation and process-count behavior remain unverified. |
| `CaptureRequest`／`CaptureRequestCoordinator`／application boundary | `Partial` for the second request-boundary slice | PrintScreen and `SecondaryInAppCommand` share one platform-neutral Core boundary; COMP-001 starts at `ResidentReady` and accepts only `ResidentReady → CaptureRequested`. Current-HEAD packaged runtime confirmed accepted first request、Busy rejection、restart reset and no Freezing／Capture side effect. Continue with the later capture slice. |
| `SupportedCapacityPolicy`、`VirtualDesktopSnapshot`、`DisplaySnapshot` | `Conforms` for the third-slice platform-neutral foundation | Four-4K source、Virtual Desktop、Selection allocation limits、negative coordinates、mixed DPI、irregular gaps and typed over-limit outcomes are implemented with synthetic contract evidence. | Preserve limits and validate again before later Selection／render allocation. |
| `CaptureSessionContext`、`FrozenDisplayFrame`、`FrozenDisplayFrameSet` | `Conforms` for the third-slice ownership foundation | One Session identity、RequestedAt、coordinate version、capacity result、cancellation and per-display immutable frame ownership are enforced; duplicate／missing／unknown／mismatched frames and partial cleanup are deterministic-tested, with current Owner Reference real frame-set metadata evidence. | Carry the same Session and frame set into later Selection and render. |
| `CaptureFreezingCoordinator` | `Partial` for the third／fourth freezing slices | Only the active accepted request can create one Session and request `CaptureRequested → Freezing`; capacity is checked before the all-display provider, complete frame-set attachment and cleanup outcomes are tested, with current Owner Reference provider/WGC runtime evidence. Fifth-slice presentation consumes the complete frame set without changing its ownership. | Preserve the freezing boundary and keep topology-change runtime separate. |
| `FrozenDisplayOverlayPlanBuilder`／`InitialSelectionCoordinator`／`CapturePresentationWorkflowCoordinator` | `Partial` for Stage 5 Owner Reference plus Stage 6A／6B／6C static slices | Builds one physical overlay descriptor per display, presents only complete frame sets, maps pointer input to normalized physical Virtual Desktop bounds, and routes valid release／Esc through COMP-001. Stage 6A adds locked move／resize／reselection with revision and rollback; Stage 6B adds `SelectionLocked → Editing`, stale Function Bar command boundaries, adjustment hide／reposition／show and Cancel cleanup; Stage 6C adds Complete gate、same-frame render／Clipboard delivery、typed stage tracing、retained Editing failures with immediate Function Bar feedback and terminal cleanup while preserving one authority. Deterministic evidence passes; the packaged Clipboard failure exposed pre-boundary DataPackage construction after the MTA correction, and the synchronous WinUI entrypoint plus complete payload boundary corrections are build／test verified, while post-fix runtime remains pending. | Execute the separately authorized Stage 6C post-fix runtime using the new fixed Artifact, then continue only after owner acceptance. |
| `WindowsFrozenDisplayOverlayCoordinator`／`WindowsFrozenDisplayFrameSetRenderer`／`FrozenPrivacyEffectRenderer` | `Partial` for Stage 6A／6B／6C and Stage 7F–7N static presentation／output | Provides one per-display physical overlay, PMA V2 placement, own frozen frame presentation, dim-outside／clear-inside mask, one session-owned native/XAML release boundary, eight physical handle surfaces and hit-test cursor mapping. Stage 6B hosts one Function Bar inside the anchor overlay; Stage 6C adds physical intersection composition into a canonical SoftwareBitmap and Clipboard delivery. Stage 7F adds Function Bar Mosaic／Blur controls and frozen-frame privacy preview; Stage 7G adds Numbered Marker `NumberBox` control and committed／draft solid-circle preview; Stage 7H adds selected-object style／delete／text-edit controls, preview adornments and per-display clipping; Stage 7I adds revision-aware Undo／Redo buttons; Stage 7J／7K add annotation-aware final rendering and explicit Complete integration; Stage 7N wires the Function Bar Save button through the same command／in-flight gate. The current-source Development MSIX was generated but signing failed with `APPX0105`／`APPX0107`, and unsigned installation returned `0x800B0100`, so packaged Stage 7H–7N runtime remains blocked／Pending. | Preserve the accepted Owner Reference Complete／paste path; keep Save runtime and Maximum four-4K separate. |

## 7. Required Correction Order

1. Resident lifecycle、direct MainWindow exit and takeover setting — deterministic evidence and Repository owner Stage 5 manual acceptance passed for the Owner Reference environment; preserve the documented Windows PrintScreen prerequisite.
2. PrintScreen entry integrated with `COMP-001` through `ResidentReady → CaptureRequested` — static implementation、deterministic tests and current-HEAD packaged runtime verification passed; preserve this boundary while continuing through the third-slice Freezing foundation.
3. Four-4K capacity policy、Frozen Virtual Desktop context、per-display frame ownership、Windows topology／per-display WGC freezing integration and `CaptureRequested → Freezing` foundation — static implementation、deterministic non-interactive evidence and current Owner Reference three-display runtime passed; cancellation interruption remains inconclusive and topology-change runtime remains pending.
4. All-display presentation、crosshair and cross-monitor initial Selection — fifth slice implementation、deterministic verification and Repository owner Owner Reference manual acceptance passed. Broader Maximum four-4K capacity remains pending.
5. Locked Selection、pointer move、edge／corner resize and reselection — Stage 6A static implementation and deterministic tests passed, including cross-Overlay release ownership and Virtual Desktop boundary cases; packaged runtime verification pending.
6. Accepted workflow state graph — Stage 6B `SelectionLocked → Editing → Cancelled` foundation implemented and deterministically tested; the post-routed-event Cancel dispatch correction is included; packaged verification pending.
7. Function Bar、Complete／Save／Cancel and placement foundation — Stage 6B／6C static implementation and deterministic tests passed, including high-contrast button policy、single-dispatch command gates、same-frame final render、transparent gap composition and bounded Clipboard delivery. Stage 7N adds production Save availability、one canonical render、PNG／Clipboard same-result commitment、picker cancellation and recoverable output failures; packaged Save、progress and focus restoration remain pending.
8. Annotation Document／Object identity／Revision foundation — Stage 7A static implementation and deterministic evidence passed; preserve the document boundary.
9. Pointer-driven Rectangle creation／preview — Stage 7B static implementation and deterministic evidence passed; object editing、other tools、annotation-aware output and runtime acceptance remain pending.
10. Pointer-driven Arrow／Line creation／preview — Stage 7C static implementation and deterministic evidence passed; object editing、other tools、annotation-aware output and runtime acceptance remain pending.
11. Pointer-driven semi-transparent Highlighter creation／preview — Stage 7D static implementation and deterministic evidence passed; object editing、other tools、annotation-aware output and runtime acceptance remain pending.
12. Pointer-driven Text creation／draft／commit／native editor — Stage 7E static implementation and deterministic evidence passed; packaged Windows／Chinese IME acceptance、style controls、object editing and annotation-aware rendering remain pending.
13. Pointer-driven Mosaic／Blur Privacy Region creation／frozen-frame preview — Stage 7F static implementation and deterministic evidence passed; packaged runtime is blocked／pending because the generated development MSIX was unsigned and no matching development certificate was available in the workspace. Object editing and annotation-aware final rendering remain pending.
14. Pointer-driven Numbered Marker creation／preview — Stage 7G static implementation and deterministic evidence passed; packaged runtime, user color／size controls, object editing, deletion, Undo／Redo and annotation-aware final rendering remain pending.
15. Pointer-driven Annotation object selection／move／resize／style／Text edit／delete — Stage 7H static implementation and deterministic evidence passed; Stage 7K now consumes the resulting immutable document through Complete, while packaged runtime remains pending because the current Development MSIX could not be signed／installed in this environment.
16. Annotation Undo／Redo、Virtual Desktop anchoring and Selection clipping — Stage 7I history and Stage 7J／7K exact physical-coordinate compositor／clipping／transparent-gap／Complete integration deterministic evidence passed; packaged runtime remains pending.
17. Complete final render、capacity revalidation、transparent gaps and Clipboard — Stage 7K integrates non-empty annotation-aware Complete; Stage 7L places publication behind the Core-owned explicit Complete／future Successful Save commitment gate, validates exact Session／Result／Selection／Annotation identity and rejects invalid or stale results without output. Stage 7N adds the production Save invocation of that Successful Save gate after matching PNG evidence. Locked restore、Release build、`260/260` filtered non-interactive tests and deterministic gate／failure recovery pass. Packaged runtime、broader four-4K evidence and performance remain pending.
18. Save As with Downloads default、PNG、same-result Clipboard and retained-file outcome — Stage 7M foundation plus Stage 7N production Save integration now provide picker-selected PNG persistence、one canonical result／ResultId through PNG／Clipboard、cancel／failure recovery and PNG retention after Clipboard failure. A dedicated retained-file reference／feedback surface and packaged/native runtime remain pending.
19. Recoverable failure preservation、stale-revision protection、performance／memory evidence and required accessibility.
20. Explicitly authorized Owner Reference、Standard and Maximum runtime verification.

Each correction task updates matrix status only after code、tests and applicable evidence exist.

## 8. Finalized Product Decisions

- MainWindow `X` exits and releases takeover; no close-to-tray behavior.
- Physical non-display gaps are transparent.
- Save As starts in Downloads and allows another destination.
- PNG is retained if later Clipboard publication fails.
- Quantitative performance and memory targets are fixed in `PRD-0006 §3`.
- Maximum source capacity is four displays、each no larger than `3840 × 2160`、total source pixels `≤ 33,177,600`.
- Virtual Desktop and output-allocation caps remain fixed in `PRD-0006 §11`.
- Owner Reference verification uses 2K primary、FHD lower at 150% scaling and left 2K.
- Keyboard-only Annotation and non-PrintScreen tool／action shortcuts are deferred.
- PrintScreen entry and Esc cancellation remain required.

No current v1 behavior is `Blocked by product decision`.

## 9. Final Conclusion

Stage 7N static evidence update: the platform-neutral annotation-aware render boundary and Windows canonical compositor remain integrated into explicit Complete for committed Rectangle、Arrow／Line、Highlighter、Microsoft JhengHei Text、Mosaic／Blur Privacy Region and Numbered Marker objects. The Core-owned `OutputCommitmentCoordinator` remains the sole source owner of Clipboard delivery; `Complete` and production `Save` use explicit authorizations with exact Session／Result／Selection／Annotation identity, while Save additionally requires matching typed PNG-write evidence. Save revalidates capacity／frame context, renders exactly one canonical result, carries the same image／`ResultId` through PNG and Clipboard, maps picker cancellation／write／Clipboard failure back to Editing and preserves the selected PNG after a post-write Clipboard failure. The filtered non-interactive suite is `260/260`; locked restore、Release x64 build and `git diff --check` passed. Limited formatter verification was run only on this slice's C# files and reports only the existing LF→CRLF `ENDOFLINE` baseline; no packaged/native Save runtime was run. Stage 7N is `Partial／runtime Pending` and Stage 7 is not complete.

The corrected Stage 5 resident lifecycle／PrintScreen／activation source and fifth-slice Owner Reference Overlay／Crosshair／initial Selection acceptance remain recorded with the documented Windows PrintScreen compatibility prerequisite. Stage 6A adds platform-neutral SelectionLocked adjustment、Stage 6B adds the Editing／Function Bar／Cancel foundation and Stage 6C adds the Complete／same-frame final render／Clipboard vertical slice while preserving `COMP-001` as the sole workflow authority. Stage 6C adds typed failure-stage tracing、immediate retained-Editing Function Bar recovery and a Windows UI／COM dispatcher boundary; after the packaged trace identified `CO_E_NOTINITIALIZED` at `SetContent` and the synchronous entrypoint correction exposed `RPC_E_CHANGED_MODE` in the MTA initializer, the source now uses synchronous WinUI STA startup with worker-thread activation redirection and creates the complete Clipboard payload and executes `SetContent`／`Flush` inside the matching Windows Runtime STA boundary. Repository owner has now confirmed one packaged Complete／paste scenario with matching selected content／position. Stage 7A adds the platform-neutral Annotation Document／Object identity／Revision foundation、Stage 7B adds pointer-driven Rectangle creation／preview、Stage 7C adds pointer-driven Arrow／Line creation／preview、Stage 7D adds pointer-driven semi-transparent Highlighter creation／preview、Stage 7E adds pointer-driven Text draft／commit／native editor foundation、Stage 7F adds pointer-driven Mosaic／Blur Privacy Region creation／frozen-frame preview、Stage 7G adds pointer-driven Numbered Marker creation／draft／commit／next-number state／preview、Stage 7H adds pointer-driven object editing、Stage 7I adds Annotation-only Undo／Redo history、Stage 7J adds the platform-neutral annotation-aware render boundary plus Windows canonical committed-object compositor with exact physical-coordinate projection／Selection clipping／transparent gaps、Stage 7K integrates that renderer into Complete with final capacity／identity／canonical-result validation、Stage 7L places all Clipboard publication behind the explicit Complete／future Successful Save commitment gate with required revision／Result identity evidence and Stage 7N integrates production Save through one canonical result／ResultId into PNG／Clipboard with typed cancellation／failure recovery. Stage 7N deterministic evidence is complete, but packaged Save／native picker runtime、broader four-4K capacity、performance and dedicated retained-file reference evidence remain pending. Stage 7 is not complete.
